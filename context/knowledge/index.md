# Knowledge Index

Structured knowledge for cross-session persistence. Each file covers a topic/domain.

| File | Topic | Key Entities | Last Updated |
|------|-------|-------------|-------------|
| code-quality.md | Refactoring patterns, daemon architecture, and deferred items | ScrollState, diffparse, sgrColor, borderedPanel, drawBorderedPanel, innerRect, file splits, GitStatus pointer bug, cursor skip-header, Alt modifier key-to-bytes, worktree removal safety guard, self-managed worktrees, diff panel line wrapping, tmux color palette, PanelLayout extraction, worktree-first task creation regression, PanelLayout width enforcement bug, SessionProvider/SessionHandle interfaces, multi-writer pattern, nil-interface gotcha, daemon IPC protocol, RingBuffer export, daemon client, chroma background compositing, injectBg, onFinish ordering, RPC timeout wrapper, daemon file logging, tab-zero-width expandTabs, daemon restart, SessionID preservation on restart, AutoStart extraction, stream-failure-auto-complete bug, StreamLost flag, isSessionAlive reachable flag, client.Get race fix, daemon health check daemonFailures, uxlog debug logging, sandbox-exec SBPL profile, SBPL /tmp symlink gotcha, sandbox-exec deny-after-allow semantics, claude.json write required for startup, ~/.claude subpath write for resume, symlink write-rule bypass, daemon binary staleness requires restart, sandbox profile temp file debugging, extra_write garbage SBPL rules, settings cursor off-by-one, remote branch resolution for worktrees, daemon cleanup race, zombie prevention, killExistingDaemon, removeIfOwnedByPID, signal.Stop, Serve/Shutdown goroutine ordering, MergeChangedFiles file explorer merge pattern, resolveGitDir worktree .git write access, PR URL detection, PRURL Task field, pr_url DB column, prURLRe regex, PRDetectedMsg, fast-exit LastOutput scan, 'o' key browser open, reviews tab, gh search prs field limits, enrichReviewDecisions, uxlog logging requirements, task rename, Task.Archived, rowArchiveHeader, archiveExpanded, archiveProject, ToggleArchive, CursorOnArchiveHeader, isInArchiveSection, groupByProject, projectTasksFiltered, argusd symlink process naming, Codex backend support, IsCodexBackend filepath.Base detection, CaptureCodexSessionID, codexStateDB state_5.sqlite, codexResumeCmd constant, CodexSessionCapturedMsg async capture flow, --last unreliable multi-session, UUID validation before command construction, Backend.ResumeCommand removed, resume_command DB column (compat only), backend-aware BuildCmd, backend selector new task form, async skill autocomplete refresh, backendform.go 3 fields, fixupBackends --yolo/--full-auto to --dangerously-bypass-approvals-and-sandbox, skipUpPastHeader, landOnLastTask, archive header cursor bug, file explorer autoExpand, parentDir helper, cursor-path-restore on row rebuild, PR list sort-order invariant, reviews tab caching (prListCooldown, SetPRs background refresh, hadData flag), KB FTS5 SQLite, kb_documents virtual table, kb_metadata, KBStore interface, MCP server internal/mcp, kb_search kb_read kb_list kb_ingest tools, inject.InjectGlobal, inject.InjectWorktree, inject/codex TOML injection, SetClaudeProjectMcpTrust, inject.SetMCPPort atomic var, Obsidian task syntax #argus, ParseTasks regex, KBConfig HTTPPort MetisVaultPath ArgusVaultPath AutoCreateTasks, kb_pending_tasks table, two-vault architecture (Metis KB + Argus tasks), FTS5 SanitizeQuery operator set, FTS5 N+1 LEFT JOIN pattern, mcp.Server.Shutdown, atomic config writes, explicit configKey parameter, filepath.Walk root error propagation, Indexer daemon wiring, TaskPreviewPanel pre-rendered cells, task preview bottom-of-history rendering, taskGitPanel, fetchTaskGitStatus, pending-resize PTY pattern, tview thread safety GetInnerRect, RPC-outside-lock pattern, OnSessionExit tui2 wiring, HandleSessionExit, ConfirmDeleteModal, modeConfirmDelete, deleteTask, pruneCompletedTasks, tui2/worktree.go (removeWorktreeAndBranch, isWorktreeSubdir), tui2 diff syntax highlighting (styledChar, highlightedLine, renderedDiffLine, highlight.go, diffrender.go, buildUnifiedDiffLines, buildSideBySideDiffLines, applyDiffBG, drawStyledLine, Chroma tokenizer), AgentHeader agent view header widget, tui2 new task form word navigation (wordboundary.go, wordLeftPos, wordRightPos, deleteWordLeft, deleteWordRight, zsh WORDCHARS), tui2 new task form skill autocomplete (acOpen, acMatches, acIdx, acScroll, acTrigger, updateAutocomplete, acMoveUp, acMoveDown, drawAutocomplete, acMaxVisible, FilterSkills), ensureCursorVisible scroll-reset fix (totalLines parameter, scrollOffset reset when content fits), projectStatusIcon aggregated project icons, tickEven animation toggle, idle wiring refreshTasksWithIDs signature, auto-navigate exitAgentView on completion, NewTaskForm word-wrap + Color237 input background + high-contrast cursor, CreateWorktree branchName return value bug, worktree cleanup os.RemoveAll unconditional, pruneWorktrees stale ref cleanup, branch inference from dir name, PR URL detection restoration (scanAndStorePRURL, prURLRe tick scan, exit scan, OnOpenPR callback, p/ctrl+p task list keys), TDD infrastructure (internal/testutil, Equal/DeepEqual/Nil/NotNil assertions, go-cmp, Makefile targets, CI coverage), escape key agent view fix (explicit handling in KeyEscape case, ResetScroll on escape write), worktree stale-ref prune fix, isValidWorktreeDir, cleanGitOutput, worktree creation uxlog deferred items, uvCellToTcellStyle attribute completeness, underline style mapping, OSC 8 hyperlink passthrough, AttrFaint→Dim Codex highlight fix, session resume wiring (GenerateSessionID, CaptureCodexSessionID, Enter-to-restart handler, --session-id/--resume lifecycle), renderLive buffer copy optimization (emuFedTotal guard, startAgentRedrawLoop idle suppression, countingAdapter test), bracket paste support (EnablePaste, PasteHandler on all text input widgets, PTY bracket paste sequences), paint cache (cachedCell, paintCacheCells, paintCacheValid, paintCacheScroll, replayPaintCache, tview screen.Clear dirty-tracking defeat), To Dos tab (ToDosView, ToDoListPanel, ToDoPreviewPanel, ToDoDetailPanel, ToDoItem, ScanVaultToDos, LaunchToDoModal, TabToDos, RefreshAsync, maxToDoFileSize), daemon crash stream-lost race fix (removeSessionStreamLost in <-rs.done path, daemonRestarting reconciliation guard, zero-value ExitInfo bug), live scrollback cache (replayEmuMaxScroll, append-only log invariant, scroll-beyond-cache rebuild), scroll acceleration (AccelScrollUp, AccelScrollDown, scrollAccelWindow, scrollAccelMax, lastScrollTime, scrollAccel), anchor-lock replay reset fix (anchorTotalLines reset on renderReplay rebuild), invalidateReplayCache helper (scroll mode transition fix, stale replayEmu + anchorTotalLines on 0→>0), fork task (ForkTaskModal, forkContext, extractForkContext, writeForkContextFiles, buildForkPrompt, executeFork, modeForkTask), todo-task association (TodoPath, TasksByTodoPath, buildToDoPrompt, ConfirmCleanupToDosModal, status-aware bullets, vault cleanup, OnStatusChange s/S toggle), default todo project (Defaults.TodoProject, srToDoProject, cycleTodoProject), task list filter (matchesFilter, handleFilterInput, ClearFilter, drawFilterInput, filterActive auto-expand, global key bypass), lazyScreen skipClear (tview screen.Clear bypass for PTY keystrokes, zero terminal I/O on idle draw), drawTaskRow cursor-fill elapsedCol dedup, rename task (db.Rename, sanitizeTaskName, modeRenameTask, RenameTaskForm, ResetDone, OnRename callback, refreshTasksLocal for rename), fork output sanitization (sanitizeForkOutput, cleanLongLine, isNoiseLine, \r normalization, NBSP normalization, inline noise patterns, VT cell concatenation), EnablePaste/EnableMouse SetScreen ordering regression, smoke_test.go SimulationScreen integration tests (simApp, wireApp, runApp, syncUI, readUI helpers), keystroke echo 16ms follow-up redraw (TotalWritten guard, skipClear interaction), remote control (vault watcher fsnotify, HTTP REST API, HeadlessCreateTask, TaskCreator injection, APIConfig, api-token bearer auth, SSE output streaming), async replay rebuild (asyncReplayRebuild, readLogTailForTask, replayRebuildPending, replayBuilding, EagerReplayBuild, OnNeedRedraw, stale paint save/restore, leakedCalls drain goroutine, statSessionLog preview cache, lastPreviewLogSize), live emu scroll fallback (fallbackEmu priority chain, scrollOffset save/restore), remote API skills autocomplete (GET /api/skills, skillJSON, handleListSkills, cachedSkills client-side cache), shared PTY sanitization (internal/sanitize, StripANSI, CleanPTYOutput, cleanLongLine, isNoiseLine, DEC private mode regex, API ?clean=1 param), auto-resume on agent view entry (onTaskSelect auto-startSession, SessionID condition, archived guard, double-start invariant), per-project sandbox UI (pfFieldSandbox, sandboxInherit/sandboxEnabled/sandboxDisabled, sandboxDenyRead/sandboxExtraWrite round-trip preservation, renderProjectDetail sandbox section, ResolveSandboxConfig merge), modal close focus restoration (closeLinkPickerModal, closeLaunchToDoModal, closeCleanupToDosModal, closeDeleteToDoModal SetFocus fix), MCP task management tools (task_create task_list task_get task_stop, SetTaskManager, TaskCreator/TaskQuerier/TaskStopper interfaces, taskToolDefs conditional exposure, truncatePromptToName, maxConcurrentCreates rate limit, taskMgmtEnabled), mouse click focus guard (guardedSetFocus, TaskPage.MouseHandler, ToDosView.MouseHandler, non-interactive panel focus steal), SBPL pseudo-terminal invalid operation fix (PTY device file-write regex rules, TestGenerateSandboxConfig_ProfileValid sandbox-exec validation test), auto-start todos (AutoStartTodos, AutoStartInterval, StartPolling, handleToggleAutoStart, polling vs fsnotify vault watcher), moveCursor boundary change suppression (defer guard, notifyCursorChange dedup), file explorer nested tree (buildChildTree, fpRow.displayName, fpIndentWidth, skipToLastChild, wasChild CursorUp fix, autoExpand indent==0 guard), configurable spinner animation (internal/spinner package, Spinner struct, Style/Label/Frames/TickInterval, activeSpinner, SetActiveSpinner, SpinnerFrame, SpinnerFrameCount, SpinnerTickInterval, ui.spinner config key, srSpinner settings row, cycleSpinner, spinnerLoop 100ms redraw, time-based updateSpinnerFrame, Progress/Dots/Braille/Classic styles), tick goroutine thread safety (QueueUpdateDraw consolidation, tickCallbackPending debounce, prScanTailSize, refreshPreview tview-goroutine contract), header notice system (Header.SetNotice, Header.ClearNotice, non-blocking prune progress, atomic.Int32 counter, orphan sweep batch unit, re-entrancy guard via notice check), spinnerLoop idle-task redraw suppression, settings scroll offset clamp | 2026-04-02 |, sgrColor, borderedPanel, drawBorderedPanel, innerRect, file splits, GitStatus pointer bug, cursor skip-header, Alt modifier key-to-bytes, worktree removal safety guard, self-managed worktrees, diff panel line wrapping, tmux color palette, PanelLayout extraction, worktree-first task creation regression, PanelLayout width enforcement bug, SessionProvider/SessionHandle interfaces, multi-writer pattern, nil-interface gotcha, daemon IPC protocol, RingBuffer export, daemon client, chroma background compositing, injectBg, onFinish ordering, RPC timeout wrapper, daemon file logging, tab-zero-width expandTabs, daemon restart, SessionID preservation on restart, AutoStart extraction, stream-failure-auto-complete bug, StreamLost flag, isSessionAlive reachable flag, client.Get race fix, daemon health check daemonFailures, uxlog debug logging, sandbox-exec SBPL profile, SBPL /tmp symlink gotcha, sandbox-exec deny-after-allow semantics, claude.json write required for startup, ~/.claude subpath write for resume, symlink write-rule bypass, daemon binary staleness requires restart, sandbox profile temp file debugging, extra_write garbage SBPL rules, settings cursor off-by-one, remote branch resolution for worktrees, daemon cleanup race, zombie prevention, killExistingDaemon, removeIfOwnedByPID, signal.Stop, Serve/Shutdown goroutine ordering, MergeChangedFiles file explorer merge pattern, resolveGitDir worktree .git write access, PR URL detection, PRURL Task field, pr_url DB column, prURLRe regex, PRDetectedMsg, fast-exit LastOutput scan, 'o' key browser open, reviews tab, gh search prs field limits, enrichReviewDecisions, uxlog logging requirements, task rename, Task.Archived, rowArchiveHeader, archiveExpanded, archiveProject, ToggleArchive, CursorOnArchiveHeader, isInArchiveSection, groupByProject, projectTasksFiltered, argusd symlink process naming, Codex backend support, IsCodexBackend filepath.Base detection, CaptureCodexSessionID, codexStateDB state_5.sqlite, codexResumeCmd constant, CodexSessionCapturedMsg async capture flow, --last unreliable multi-session, UUID validation before command construction, Backend.ResumeCommand removed, resume_command DB column (compat only), backend-aware BuildCmd, backend selector new task form, async skill autocomplete refresh, backendform.go 3 fields, fixupBackends --yolo/--full-auto to --dangerously-bypass-approvals-and-sandbox, skipUpPastHeader, landOnLastTask, archive header cursor bug, file explorer autoExpand, parentDir helper, cursor-path-restore on row rebuild, PR list sort-order invariant, reviews tab caching (prListCooldown, SetPRs background refresh, hadData flag), KB FTS5 SQLite, kb_documents virtual table, kb_metadata, KBStore interface, MCP server internal/mcp, kb_search kb_read kb_list kb_ingest tools, inject.InjectGlobal, inject.InjectWorktree, inject/codex TOML injection, SetClaudeProjectMcpTrust, inject.SetMCPPort atomic var, Obsidian task syntax #argus, ParseTasks regex, KBConfig HTTPPort MetisVaultPath ArgusVaultPath AutoCreateTasks, kb_pending_tasks table, two-vault architecture (Metis KB + Argus tasks), FTS5 SanitizeQuery operator set, FTS5 N+1 LEFT JOIN pattern, mcp.Server.Shutdown, atomic config writes, explicit configKey parameter, filepath.Walk root error propagation, Indexer daemon wiring, TaskPreviewPanel pre-rendered cells, task preview bottom-of-history rendering, taskGitPanel, fetchTaskGitStatus, pending-resize PTY pattern, tview thread safety GetInnerRect, RPC-outside-lock pattern, OnSessionExit tui2 wiring, HandleSessionExit, ConfirmDeleteModal, modeConfirmDelete, deleteTask, pruneCompletedTasks, tui2/worktree.go (removeWorktreeAndBranch, isWorktreeSubdir), tui2 diff syntax highlighting (styledChar, highlightedLine, renderedDiffLine, highlight.go, diffrender.go, buildUnifiedDiffLines, buildSideBySideDiffLines, applyDiffBG, drawStyledLine, Chroma tokenizer), AgentHeader agent view header widget, tui2 new task form word navigation (wordboundary.go, wordLeftPos, wordRightPos, deleteWordLeft, deleteWordRight, zsh WORDCHARS), tui2 new task form skill autocomplete (acOpen, acMatches, acIdx, acScroll, acTrigger, updateAutocomplete, acMoveUp, acMoveDown, drawAutocomplete, acMaxVisible, FilterSkills), ensureCursorVisible scroll-reset fix (totalLines parameter, scrollOffset reset when content fits), projectStatusIcon aggregated project icons, tickEven animation toggle, idle wiring refreshTasksWithIDs signature, auto-navigate exitAgentView on completion, NewTaskForm word-wrap + Color237 input background + high-contrast cursor, CreateWorktree branchName return value bug, worktree cleanup os.RemoveAll unconditional, pruneWorktrees stale ref cleanup, branch inference from dir name, PR URL detection restoration (scanAndStorePRURL, prURLRe tick scan, exit scan, OnOpenPR callback, p/ctrl+p task list keys), TDD infrastructure (internal/testutil, Equal/DeepEqual/Nil/NotNil assertions, go-cmp, Makefile targets, CI coverage), escape key agent view fix (explicit handling in KeyEscape case, ResetScroll on escape write), worktree stale-ref prune fix, isValidWorktreeDir, cleanGitOutput, worktree creation uxlog deferred items, uvCellToTcellStyle attribute completeness, underline style mapping, OSC 8 hyperlink passthrough, AttrFaint→Dim Codex highlight fix, session resume wiring (GenerateSessionID, CaptureCodexSessionID, Enter-to-restart handler, --session-id/--resume lifecycle), renderLive buffer copy optimization (emuFedTotal guard, startAgentRedrawLoop idle suppression, countingAdapter test), bracket paste support (EnablePaste, PasteHandler on all text input widgets, PTY bracket paste sequences), paint cache (cachedCell, paintCacheCells, paintCacheValid, paintCacheScroll, replayPaintCache, tview screen.Clear dirty-tracking defeat), To Dos tab (ToDosView, ToDoListPanel, ToDoPreviewPanel, ToDoDetailPanel, ToDoItem, ScanVaultToDos, LaunchToDoModal, TabToDos, RefreshAsync, maxToDoFileSize), daemon crash stream-lost race fix (removeSessionStreamLost in <-rs.done path, daemonRestarting reconciliation guard, zero-value ExitInfo bug), live scrollback cache (replayEmuMaxScroll, append-only log invariant, scroll-beyond-cache rebuild), scroll acceleration (AccelScrollUp, AccelScrollDown, scrollAccelWindow, scrollAccelMax, lastScrollTime, scrollAccel), anchor-lock replay reset fix (anchorTotalLines reset on renderReplay rebuild), invalidateReplayCache helper (scroll mode transition fix, stale replayEmu + anchorTotalLines on 0→>0), fork task (ForkTaskModal, forkContext, extractForkContext, writeForkContextFiles, buildForkPrompt, executeFork, modeForkTask), todo-task association (TodoPath, TasksByTodoPath, buildToDoPrompt, ConfirmCleanupToDosModal, status-aware bullets, vault cleanup, OnStatusChange s/S toggle), default todo project (Defaults.TodoProject, srToDoProject, cycleTodoProject), task list filter (matchesFilter, handleFilterInput, ClearFilter, drawFilterInput, filterActive auto-expand, global key bypass), lazyScreen skipClear (tview screen.Clear bypass for PTY keystrokes, zero terminal I/O on idle draw), drawTaskRow cursor-fill elapsedCol dedup, rename task (db.Rename, sanitizeTaskName, modeRenameTask, RenameTaskForm, ResetDone, OnRename callback, refreshTasksLocal for rename), fork output sanitization (sanitizeForkOutput, cleanLongLine, isNoiseLine, \r normalization, NBSP normalization, inline noise patterns, VT cell concatenation), EnablePaste/EnableMouse SetScreen ordering regression, smoke_test.go SimulationScreen integration tests (simApp, wireApp, runApp, syncUI, readUI helpers), keystroke echo 16ms follow-up redraw (TotalWritten guard, skipClear interaction), remote control (vault watcher fsnotify, HTTP REST API, HeadlessCreateTask, TaskCreator injection, APIConfig, api-token bearer auth, SSE output streaming), async replay rebuild (asyncReplayRebuild, readLogTailForTask, replayRebuildPending, replayBuilding, EagerReplayBuild, OnNeedRedraw, stale paint save/restore, leakedCalls drain goroutine, statSessionLog preview cache, lastPreviewLogSize), live emu scroll fallback (fallbackEmu priority chain, scrollOffset save/restore), remote API skills autocomplete (GET /api/skills, skillJSON, handleListSkills, cachedSkills client-side cache), shared PTY sanitization (internal/sanitize, StripANSI, CleanPTYOutput, cleanLongLine, isNoiseLine, DEC private mode regex, API ?clean=1 param), auto-resume on agent view entry (onTaskSelect auto-startSession, SessionID condition, archived guard, double-start invariant), per-project sandbox UI (pfFieldSandbox, sandboxInherit/sandboxEnabled/sandboxDisabled, sandboxDenyRead/sandboxExtraWrite round-trip preservation, renderProjectDetail sandbox section, ResolveSandboxConfig merge), modal close focus restoration (closeLinkPickerModal, closeLaunchToDoModal, closeCleanupToDosModal, closeDeleteToDoModal SetFocus fix), MCP task management tools (task_create task_list task_get task_stop, SetTaskManager, TaskCreator/TaskQuerier/TaskStopper interfaces, taskToolDefs conditional exposure, truncatePromptToName, maxConcurrentCreates rate limit, taskMgmtEnabled), mouse click focus guard (guardedSetFocus, TaskPage.MouseHandler, ToDosView.MouseHandler, non-interactive panel focus steal), SBPL pseudo-terminal invalid operation fix (PTY device file-write regex rules, TestGenerateSandboxConfig_ProfileValid sandbox-exec validation test), auto-start todos (AutoStartTodos, AutoStartInterval, StartPolling, handleToggleAutoStart, polling vs fsnotify vault watcher), moveCursor boundary change suppression (defer guard, notifyCursorChange dedup), configurable spinner animation (internal/spinner package, Spinner struct, Style/Label/Frames/TickInterval, activeSpinner, SetActiveSpinner, SpinnerFrame, SpinnerFrameCount, SpinnerTickInterval, ui.spinner config key, srSpinner settings row, cycleSpinner, spinnerLoop 100ms redraw, time-based updateSpinnerFrame, Progress/Dots/Braille/Classic styles) | 2026-03-30 |

## Coverage Map

Which context files are captured in knowledge:

| Context File | Knowledge File(s) | Coverage |
|-------------|-------------------|----------|
| context/plans/daemon-architecture.md | code-quality.md (Daemon Architecture section) | Implementation patterns, IPC design, gotchas |
| context/plans/sandbox-worktrees.md | code-quality.md (Sandbox section) | sandbox-exec SBPL profile, BuildCmd signature, sandbox config lifecycle, SBPL gotchas |
| context/research/daemon-lifecycle-flows.md | code-quality.md (Daemon Cleanup Race section) | All startup/shutdown/restart flows, goroutine ordering, race analysis |
| context/research/terminal-runtime-notes.md | key-learnings.md (tui2 entries) | Library evaluation (tcell, tview, x/vt), migration complete |
| context/plans/2026-03-18-terminal-passthrough-plan.md | key-learnings.md (tui2 entries), code-quality.md (Phase 2 section) | All phases complete: tcell/tview runtime is sole UI |
| context/knowledge/reference-bt-migration.md | reference-bt-migration.md | Pre-tcell BT reference commit (5b8d560), old file listing |
