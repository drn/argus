# Knowledge Index

Non-obvious invariants and gotchas, split by topic. Read the relevant file when working in that area.

| File                                                 | Topic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Bullets |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| [gotchas/daemon-rpc.md](gotchas/daemon-rpc.md)       | Daemon lifecycle, RPC timeouts, reconciliation races, session resume, Claude /clear recapture (NeedsSessionRecapture, claudesession.List delegation), binary staleness, self-update, launchd auto-start, launchd PATH for ~/.local/bin/claude, stream Since offset, AddWriterFromTolerant, inputLoop paste-boundary flush, *.test fork-bomb backstop, singleton flock anti-split-brain, PR poller runPRPoller socketless shutdown test + eligibility/concurrency cap, evidence-based task completion (ExitInfo.CleanExit() single predicate: Complete only on observed clean exit; crash/stop/fast-fail/inference → InReview), reconcile→InReview never Complete, removeSession forces StreamLost on GetExitInfo RPC failure, reconcile-vs-Complete race closed by onFinish-before-delete(sessions) ordering, hera worker finish policy (BUG-050: worker-bound→InReview+meta:hera.ready_to_close; TWO triggers via shared db.RollHeraWorkerToReview — primary hera_status(done) for idle workers + exit-hook backstop; in_progress-guarded/idempotent; all flip sites mirror; pendingRestart-skip respected), startup hera-binding reconciliation (task_missing, keyed on row existence not session liveness), session-supervisor P1 (dark binary in package daemon mounting sessionCore; independent supervisor.lock/.sock/.pid trio via P0 path-param helpers; exit-caching-only onFinish preserving #707; cfgFn-not-DB; versioned Hello/ProtocolVersion under "Daemon" service; supervisor.log slog/log guards, no Dup2); session-supervisor P2 (daemon-as-supervisor-client behind default-OFF cfg.Supervisor.Enabled; agent.SessionRunner superset swap keeps OFF byte-identical; injected SupervisorClient + UseSupervisorRunner since daemon can't import client; ONE handleSessionExit sink fed by both modes, StreamLost-caches-not-skips + GetExitInfo-fail⇒StreamLost backstop on the new supervisor→daemon boundary; mode-aware cleanup never StopAlls supervisor agents; recapture stays daemon-side on the relay; cfgFn config not StartReq; RemoteSession fan-out double-proxy + ListSessionInfo relay; KickRerender RPC ProtocolVersion 1→2; AutoStartSupervisor Setsid + SupervisorProtocolMatch never-restarts-live-supervisor-on-skew), session-supervisor P3 (re-attach live sessions on daemon bounce, ON-mode only: single mode-aware Daemon.ReconcileOnStartup replaces inline reconcile trio; reattachSupervised order = query live set→Get-each-to-arm-relay→ReconcileStaleSessionsExcept-flips-only-true-orphans→signal-only-orphans; Get-loop load-bearing for #707-across-bounce not just replay; Running()==nil RPC-fail skips reconcile vs non-nil-empty all-orphans reconciles; ReconcileStaleSessionsExcept reusable primitive + ReconcileStaleSessions thin wrapper + sendBounceSignals shared loop; hera bindings survive keyed-on-row-existence; OFF byte-identical), session-supervisor P4 (default flipped ON in config.DefaultConfig mirroring hera.enabled — db/config.go parse unchanged, absent-key inherits true; in-process OFF path RETAINED one release as rollback via explicit supervisor.enabled=false DB or config.toml-wins-over-DB, NOT deleted; operational model = bounce daemon freely/re-attaches vs rare supervisor restart SIGHUPs agents; cold-start ordering daemon-auto-starts-supervisor + *.test fork-bomb backstop = TestSupColdStart isolation proof; OFF→ON one-time migration discardStaleLiveTasksFile prevents later ON→OFF stale double-fire, TestReconcileOnStartup_Supervised_FirstStartAfterOffRun; #707 cache-vs-EOF relay race FIXED (pre-existing on master, P4 e2e caught it): handleStream must awaitExitInfoCached before returning or handleConn's defer-close beats onFinish's cache → GetExitInfo "not found" → empty ExitInfo → CleanExit→Complete on a crash; GetExitInfo-RPC-FAILURE backstop doesn't cover RPC-success-empty-payload; general to daemon→TUI OFF too; TestSupCrashRelayErr asserts Err!=""; RemoteSession.close() sync.Once idempotent like Client.closeOnce, TestH_CloseIdempotent), callWithTimeout nil-rpc/nil-receiver guard (dispatch goroutine deref of nil c.rpc = unrecoverable detached-goroutine SIGSEGV; ErrClientClosed not panic; Close() also nil-rpc-guarded for symmetry; rpc never reassigned so prod-safe but test-scaffold/teardown reachable; NOT a use-after-close race—concurrent Call+Close is safe; TestC_CallNoRPC)                                                                                                                                                                                                                                                                                                       | 87      |
| [gotchas/pty-terminal.md](gotchas/pty-terminal.md)   | PTY sizing, x/vt emulator, ring buffer, replay cache, paint cache, lazyScreen, test concurrency, ESC-boundary alignment, live rebuild from log tail, paintEmu trailing-row blank, monotonic firstByteOffset, scrollOffset clamp on rebuild, waitLoop close-after-drain order, rerender unchanged-cols gate, rerender cache invalidation, rerender blocked-on-prompt gate, OSC 0x9C-in-UTF8 strip filter, persistent preview emulator (PreviewVT: reuse-via-RIS, no-scrollback), plugin terminalpane cursor sync (force hidden before consumer start)                                                                                                                                                                           | 59      |
| [gotchas/ui-threading.md](gotchas/ui-threading.md)   | tview thread safety, tick goroutine rules, lazyScreen fill invariant, paste/input batching, OnBranchChange contract, EventFocus drift recovery, fallback options (B/C) if callbacks keep leaking                                                                                                                                                                                                                                                                                                                                                                                                   | 22      |
| [gotchas/sandbox.md](gotchas/sandbox.md)             | macOS sandbox-exec SBPL profiles, symlink resolution, allowed paths, Chrome support dir for Playwright, AppleEvent allowlist, Messages.app legacy alias, picker modal                                                                                                                                                                                                                                                                                                                                                                                                                              | 25      |
| [gotchas/worktree.md](gotchas/worktree.md)           | Worktree creation ordering, transactional CreateAndStart, cleanup, path validation, stale ref pruning, missing-worktree pre-flight, delete entry-point parity, safe-name slash stripping, orphan-sweep ancestor guard                                                                                                                                                                                                                                                                                                                                                                              | 19      |
| [gotchas/keybindings.md](gotchas/keybindings.md)     | Key routing, ctrl sequences, tcell modifier quirks, agent view navigation, plugin-view full surrender + double-Ctrl+Q failsafe, plugin help overlay, keyenc single-source-of-truth + byte-identical invariant + mod-7 Cmd+arrow round-trip, control-frame read-pump threading via QueueUpdateDraw, control-dispatch test-deadlock, plugin resize-envelope reconciliation (post-layout only, connReady+laidOut gating, drift re-send), ctrl+r Claude session switcher + live-switch via pendingRerenderRestart, ctrl+k task switcher (sync build from UI-thread caches, caller-sorted needs-input-first, routes via onTaskSelect), plugin-view reconnect (onClose explicit-close suppression, Esc-exit only while reconnecting, pure backoff + nowFn message flip, laidOut survives resume)                                                                                                                                                                                                                                                                       | 34      |
| [gotchas/tasklist-ui.md](gotchas/tasklist-ui.md)     | Task list cursor, modals, focus guards, filter, archive, pinned, spinner, row rendering, form done-flag reset, needs-input icon, agent attention bar, sticky needs-input across idle gate, ❯+NBSP prompt anchor, decoration-line skip, PR indicator cell reclaims space when no actionable PR + orthogonal to in_review, filter-picker Draw width clamp (shared taskswitcher/sessionpicker/fuzzylinkpicker narrow-terminal panic guard)                                                                                                                                                                                                                                                                                                                                                          | 52      |
| [gotchas/web-remote.md](gotchas/web-remote.md)       | SPA + REST API + service worker, network binding (Tailscale-only), EventSource auth, xterm.js, HTML escaping, virtual keys, detail-view layout, files-view overlay, prompt modal, input history, link picker, new task form defaults, share-project default, file uploads, offline view, Web Push, per-device tokens, settings endpoints, share target + iOS Shortcut helper, test harness, idle task status, daemon-side status flip on session exit, static-output replay, mobile compose bar, key bar, skill autocomplete, task-list search, diff wrap & stacked file panels, full-history scrollback for active tasks, SPA JS reference resolution test, global hotkeys, session artifacts (manifest-scoped serving, X-Frame-Options override, blob+sandbox token isolation, client-side markdown), PR badge from cached pr_state (daemon-only fetch, keep-stale), compose split text→CR send (paste-batch bug class), artifact Open=full-screen overlay vs name=paneled, makeArtifactNode single-source sandbox flags | 187     |
| [gotchas/misc.md](gotchas/misc.md)                   | DB patterns, Go idioms, Codex, Pi/ollama prelaunch, MCP, PRs, file explorer, quick-add, scheduled tasks, links, task auto-naming, settings two-pane UI, plugin-scoped token tagging, task_meta sidecar, plugin settings registry (PR 7), Claude session discovery (folder encoding, JSONL parse, metadata reduction), config.toml override layer (precedence, DB-derived path, partial-map zeroing, mtime live-reload), hera plugin contract (reliable pane delivery: POST /notify + DELETE /notify/:id), model selection (--model injection scoping, codex-resume re-append, per-start resolution), wall-clock duration clamp (Task.Elapsed floors negatives from NTP/sleep skew)                                                                                                                                                                                                                                                                 | 143     |
| [gotchas/orchestration.md](gotchas/orchestration.md) | depswatcher tick loop, depends_on CreateAndStart short-circuit, task_set_result opaque-to-daemon contract, ARGUS_TASK_ID env export, (name,project) idempotency, schema column ordering, plan_slug, link/unlink/halt, FindCycle path, defensive copy in orch.Link, hera schema/store M1 (FK-pragma-required cascade, NULL-not-empty partial-index cols, per-(task,orchestrator) multi-binding + ErrHeraAmbiguous, delete-ends-bindings/archive-doesn't hook, transactional role+binding create), hera M4 (born-bound spawn = CreateAndStart + AfterPersist LIFO hook; auto-adopt owns binding rows only / depswatcher owns sessions; ground-truth re-derivation not events-ring; D4 exactly-one; spawn project from caller task not role.ArgusProject), hera M5 (subtree TLDR roll-up: SubtreeOrchIDs multi-binding BFS bridge + visited-set cycle guard + archived-prunes-descendants; tree⊥DAG; tree_read_cursors ON DELETE CASCADE BUG-034; advisory per-role cursor auto-advance-unless-explicit-since; HeraTreeUpdatesSince TLDR-only LIMIT 200 max-id nextCursor; hera_get_messages subtree access expansion)                                                                                                                                                                                                                                                                                  | 35      |
| [gotchas/dag-rendering.md](gotchas/dag-rendering.md) | Sugiyama-lite layout, rune-vs-byte truncation, single-line edge chars, web/TUI layout parity, branch-change callback for DAG widget, page-wrapper focus, TUI orphan/archived filter, M7 embedding (SetTitle retitle/suppress, OnBranchChange log-only both surfaces, SetDAGNodeProvider seam + scopeTasksToOrch orchestrator-scope, no projection fork), M7 link/unlink pickers (real not stubs, shared legacy+hera, candidates not orch-scoped, orch.Link internal FindCycle->CycleError rejection, modeHeraPicker + TaskSwitcherModal.SetTitles, []rune title-loop fix)                                                                                                                                                                                                                                                                                                                                                                                        | 23      |
| [gotchas/hera-view.md](gotchas/hera-view.md)         | Native Hera view M6a/M6b (`internal/tui/hera`): TabDAG→TabHera relabel keeps enum slot + `2` hotkey, dag page kept for M8 fallback; structural multi-binding fan-out (no per-task dedupe); freelance-kind roles hoisted to own section; ready_to_close from task_meta wins over status icon; goroutine-free debounced Refresher on UI thread (DB reads OK, not git); Machine() not Focus() (tview.Primitive collision); rail nav = j/k/Up/Down + Space (Left/Right freed — global tab-switch via arrows REMOVED, tab nav is 1/2/3 only); no Sync + full-rect coverage. M6b: panes fed from in-process runner ring (TerminalPane polls, NOT SSE/AddWriter — proxy not ported); coord-vs-agent session rule (HERA pane=selected orch's coordinator always; right region=Details for coordinator / AGENT terminal for worker); multi-binding disambiguated by role.OrchID via Rail.Selection/Model.OrchByID (SelectionContext = 6c seam); PTY size align = same TerminalPane + ForceResyncPTY on bind + SyncPanes off-thread (off-tab no-op via zero pendingResize); main-thread-safe reads only; reconcileSessions does nil→live upgrade; doRefresh re-applySelection after SetModel; PR mark from "pr" namespace url; SetBorderTitle only agent-surface change. M6c: thin mutation layer (`ops.go` Ops+MutateStore over M1 methods; spawn reuses shared agent.SpawnHeraWorker — daemon/mcp now thin adapters); rail keyset via HeraPage OnXxx callbacks (DAG-widget pattern, not injected mutator); multi-binding isolation (ops act on Selection's role/orch, delete-task only when sole binding, orch-delete preserves argus tasks); s/S step HERA ROLE status ladder not argus workflow (done+worker→RollHeraWorkerToReview); n/J/C/Ctrl+R/Ctrl+Z OMITTED w/ rationale; modeHeraInput/modeHeraConfirm early-dispatched; generic modal.ConfirmModal + generalized NewInputForm(title); remote=nil heraOps=inert keys. M7: Details region 2 modes (worker->terminal, coordinator->STACKED roster-over-DAG via drawDetailsRegion, no toggle — `g`/subMode/toggleDetailsSubMode/DetailsSubMode GONE; roster sized ContentHeight cap h/2, DAG fills rest, DAG sole interactive surface); embedded DAG orchestrator-scoped (DAGNodeProvider seam + scopeTasksToOrch, NOT subtree, reuses dagNodesFromTasks); DAG() exposes legacy widget, App wires same OnEnter/OnLink/OnUnlink/OnHalt; retitle " Dependencies " + OnBranchChange log-only + no Sync; rebuildDAG on applySelection whenever detailsMode; halt+link/unlink refresh heraPage too | 31      |
| [gotchas/messaging.md](gotchas/messaging.md)         | `task_messages` caps (64 KiB body / 500 unread / 50/min), self-send rejection, recipient existence check, reliable-notify delivery (single-writer, Ctrl+U pre-clear, CR not LF, 5-min deadline, ack-cancels), archive cleanup, `task_ask` polling, REST send (single-tier auth), test patterns; hera M2 (read_at NULL invariant, varied-sender inbox-fill, enqueue-time delivery stamp, hera: delivery-ID prefix, eager inbox cancel, doorbell trust boundary)                                                                                                                                                                                                                    | 34      |
| [gotchas/remote-tui.md](gotchas/remote-tui.md)       | `--remote URL --token TOKEN` mode: apiclient + apistore architecture, two compile-time assertions, four TUI sites that type-assert to *db.DB for local-only ops, raw endpoints for full model.Task round-trip, 30s config refresher, daemon-admin actions that don't apply remotely                                                                                                                                                                                                                                                                                                                | 13      |
| [gotchas/events.md](gotchas/events.md)               | Events ring + SSE stream: emission-outside-mu invariant, subscribe-before-snapshot fencing, sink save/restore in tests, ring eviction shape, idle watcher unconditional-run, task.completed double-emit, session.needs_input daemon-authoritative idle-gated sticky watcher, no hera adopt/reconcile loop on events ring (sink is API-gated; re-derive ground truth)                                                                                                                                                                                                                                                                                                                          | 7       |

## Cross-cutting Rules

| File                     | Purpose                                                                                                                               |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| [testing.md](testing.md) | Coverage gate, stdlib idioms, synctest, SimulationScreen, httptest, daemon mocking, exclusion list. **Read before writing any test.** |

## Other Files

| File                                                   | Purpose                                                      |
| ------------------------------------------------------ | ------------------------------------------------------------ |
| [code-quality.md](code-quality.md)                     | Historical refactoring log (archive — not loaded by default) |
| [reference-bt-migration.md](reference-bt-migration.md) | Pre-tcell BT reference commit (5b8d560)                      |
