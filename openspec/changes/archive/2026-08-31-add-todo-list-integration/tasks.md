## Tasks

- [x] `internal/todo`: define `Item`, `Backend` interface, and a name→constructor registry (`things3` registered; unknown names rejected).
- [x] `internal/todo/things3`: AppleScript adapter (`osascript` subprocess) implementing Create/List/Update/Complete/Delete against Things 3's to-do class; runtime OS check (`newForOS`, injectable for tests); clear error when Things 3 isn't installed/running.
- [x] `internal/config`: add `TodoConfig`/`Things3Config`, wired into `Config` and the existing config-value/config.toml overlay reconciliation (mirrors `kb.enabled`/`api.enabled`).
- [x] `internal/mcp/server.go`: `todo_*` tool defs (mirroring `task_*` shape/description conventions), `SetTodoManager`/`TodoConfigStore`, `resolveTodoBackend`/`todoToolsActive()` gates in `handleToolsList`/`handleToolsCall`. Turned out simpler than planned: no mutex-guarded runtime swap was needed — `*db.DB.Config()` already re-reads the DB (and re-applies the config.toml overlay) on every call, so resolving the backend fresh on each `tools/list`/`tools/call` gets the live-update requirement for free, matching every other `Set*Manager`'s existing wiring shape.
- [x] `internal/daemon`: wire the todo backend once at startup (`mcpSrv.SetTodoManager(d.db)`, inside the existing `if cfg.KB.Enabled` block) — no separate re-wire-on-change path needed, per the simplification above. Blank-imports `internal/todo/things3` for its factory-registration side effect.
- [x] `internal/tui/settings.go`: new `catTodo` "To-Do List" section — backend picker (cycles none/things3/…) + Things 3 Project/Tag fields, following the existing settings-row/save patterns; persists via `SetConfigValue`. Deliberately carries no "(restart required)" hint anywhere, unlike KB/API.
- [x] Tests: `internal/todo` backend interface + registry (100% coverage); `internal/todo/things3` (fake `osascript` runner, real Things 3 never touched, 94.6% coverage); `internal/mcp` tool gating incl. a live no-restart-required test (`TestToolsList_TodoAppearsLiveNoRestart`); `internal/tui/settings_test.go` cycle/edit/paste/no-restart-hint tests. Full-suite `make test-cover-gate` passes at 88.5% filtered coverage (floor: 88%).
- [x] `context/knowledge/gotchas/todo-integration.md`: documents the live-resolution design, unknown-backend-is-no-backend treatment, no-local-persistence, the Things 3 macOS-only/stateless/move-to-Trash/delimiter/existsGuard specifics, and the Settings edit-in-flight Refresh guard. Indexed in `context/knowledge/index.md`.
- [x] README: added the To-Do List MCP tools table and the `[todo]`/`[todo.things3]` config reference rows. Top-half README left untouched per the pillar-class-only rule.
- [x] Explicit named follow-up (not silent): web SPA / macOS Settings UI for todo-backend configuration is named as a Non-Goal in `proposal.md`, per the Frontend Parity rule — not implemented in this change.
- [x] Archive this change into the base `todo-list-integration` spec in the same PR (`openspec archive add-todo-list-integration`), before merge.
